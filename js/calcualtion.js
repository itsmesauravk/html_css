const readline = require('readline');

number = () => {
    const num1 = prompt("Enter the first number : ")
    const num2 = prompt("Enter the ssecond number : ")
    result = num1 + num2
    return result
}
number() 